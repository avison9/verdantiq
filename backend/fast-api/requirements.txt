# DEPRECATED — dependencies are now managed via pyproject.toml + uv
# See: https://github.com/astral-sh/uv
#
# To install production deps:   uv sync --no-dev
# To install all deps (+ dev):  uv sync
#
# This file is kept only for tools that cannot read pyproject.toml.
# Do not add new dependencies here.
